#import "../lib.typ": *
#show: conf.with(
    author: "BJHH2005,xhkzdepartedream,kiwiizzz,Zoomy",
    title: "Principles of Compilation · chapter1",
    cover: false,
    sidebar: false,
)

= 第1章 编译程序概述

== 程序的翻译

#definition[翻译程序（Translator）][
    将某种语言（源语言 Source Language）书写的程序，转换为与之*逻辑等价*的另一种语言（目标语言 Target Language）程序的软件系统。
]

=== 编译 vs. 解释

程序的翻译通常有两种基本执行模式：

#table(
    columns: (1fr, 2fr, 2fr),
    align: (center, left, left),
    table.header([*对比维度*], [*编译方式（Compilation）*], [*解释方式（Interpretation）*]),
    [基本机制], [翻译与运行*严格分阶段*（先整篇翻译，后独立运行）], [按动态执行顺序*逐句分析并立即执行*],
    [目标代码], [*生成独立的目标程序*（机器码/汇编代码）], [*不生成目标代码*，直接产出运行结果],
    [执行效率], [*高*（目标码直接由硬件执行，可多次重用）], [*低*（每次运行均需重复词法/语法解析）],
    [灵活性 / 调试], [较弱（改动需重新编译整个模块）], [*强*（交互性好，错误能精确定位至源码行）],
    [典型代表], [C、C++、Rust、Go], [Python、JavaScript、Shell],
)

#remark[根本区别][*是否生成目标代码*是区分编译与解释的本质分界线。]

=== 混合模式与 JIT

现代语言常采用“编译 + 解释”的混合机制（以 Java 为典型）：
+ *前端编译*：Java 源码 (`.java`) 由 `javac` 一次性编译为与机器无关的字节码 (`.class`)。
+ *后端执行*：JVM（Java 虚拟机）解释执行字节码；配合 *JIT（Just-In-Time 即时编译器）*，在运行期动态将高频调用的热点字节码直接编译为本地物理机器指令，兼顾跨平台性与执行效率。

#figure(
    image("../assets/语言处理.png", width: 45%),
    caption: [源程序的执行历程：两阶段（编译-运行）与三阶段（编译-汇编-运行）],
)

== 编译器的工作内容与阶段

编译器核心任务是实现：*高级语言源程序 $arrow.r$ 等价的低级语言目标程序*。逻辑上划分为六大核心阶段与两大支撑子系统。

#figure(image("../assets/编译程序的工作流程.png", width: 75%), caption: [编译程序的阶段构成与交互流程])

=== 核心阶段全流程

+ *词法分析（Lexical Analysis / Scanner）*
    #definition[词法分析][把字符流切成一个一个有意义的“单词”/最小语法单位，称为token，并识别出每个token的属性。]
    #example[a = b + 3 \* c => ID  ASSIGN  ID  PLUS  NUM  MUL  ID  SEMICOLON]

+ *语法分析（Syntax Analysis / Parser）*
    #definition[语法分析][依据语言的*语法规则*，验证 Token 序列是否构成合法的语法范畴（表达式、语句等），构造*语法分析树（Syntax Tree）*。]
    - 同时暴露运算优先级与嵌套关系（如识别出先乘后加）。
    - #figure(image("../assets/语法规则.png", width: 60%))

+ *语义分析（Semantic Analysis）*
    负责判断程序在“意义”上是否合理。

    包括类型检查与隐式转换、变量未声明/重复声明检测、作用域判定、函数实参与形参匹配。

    *静态 vs. 动态*：
    - *静态语义*：编译期可确定的语义（如 `float a = 1.0; int b; b = a;` 类型兼容性检查）。
    - *动态语义*：必须在运行期结合具体数据才能体现的逻辑（如除以零、数组越界、空指针解引用）。

    以 $a := b + c * 2$ 为例，若 $b, c$ 为实型，发现常数 $2$ 为整型，插入类型转换节点：`IntToReal(2)`。

    #remark[语法错误与语义错误][
        ```
        a = + * b;
        ```

        ```
        int a;
        string b;
        a = b;
        ```
    ]

+ *中间代码生成（Intermediate Code Generation）*
    将语法/语义树转化为语法结构简单、独立于具体机器的中间表示（IR，如三地址码、四元式、抽象语法树）。
    #unim[如果语义分析时把源程序表示成中间形式而不是表示成目标指令，则由本部分完成从中间形式到目标指令的转换。如果语义分析时，已直接生成目标指令，则无需另外再做代码生成工作。

        目标指令可能是绝对指令代码，或者可重新定位的指令代码，或者汇编指令代码该阶段的工作有赖于硬件系统结构和机器指令含义。]

    例如：
    ```text
    t1 := IntToReal(2)
    t2 := id3 * t1
    t3 := id2 + t2
    id1 := t3
    ```

+ *中间代码优化（Intermediate Optimization）*

    依据程序的*等价变换规则*，改善中间代码，压缩执行时间与存储占用。

    *典型手段*：常量折叠（Compile-time Evaluation）、公共子表达式消除、死代码消除、循环不变式外提。


+ *目标代码生成（Code Generation）*

    将优化后的中间代码映射为目标机指令，完成存储分配与寄存器调度。

    *指令形式*：绝对机器代码、可重定位机器代码或汇编语言代码。

=== 辅助支撑系统

除了上面六个阶段，编译器还有两条贯穿全程的支撑线索。

一条是符号表管理（Symbol Table Management）。源程序里出现的每个标识符，连同它的类型、作用域、存储单元地址（或偏移量），都登记在符号表里；除代码优化阶段外，其余各阶段随时从中读取或回填属性。

一条是出错处理（Error Handling），负责准确定位错误位置并输出诊断信息。按发现的时机，错误大致分成三类：词法上的非法字符、写错的关键字（`3a` 不是合法标识符）；语法上的括号不匹配、缺分号、结构不合法（`a = + * b`）；静态语义上的类型不匹配、标识符未声明、操作数不合法。

=== 语言规则的形式化表示（BNF 与 EBNF）

语法与词法规则普遍使用 Backus-Naur 范式（BNF）及其扩展（EBNF）精确描述：
- `::=` ：定义为（is defined as）
- `|` ：或者（or）
- `[ x ]` ：$x$ 可选（出现 0 次或 1 次）
- `{ x }` ：$x$ 重复 0 次至多次
- `( y | z )` ：分组组合，等价于 $y$ 或 $z$

#example[经典标识符与无符号常数的 EBNF 定义][
    ```text
    <标识符>     ::= <字母> { <字母> | <数字> }
    <无符号常数> ::= <数字> { <数字> }
    <字母>       ::= a | b | ... | z | A | ... | Z
    <数字>       ::= 0 | 1 | 2 | ... | 9
    ```
]

== 编译程序的结构

=== “分析—综合”哲学模型

编译逻辑分为两大对称阶段：

#table(
    columns: (1fr, 2fr, 2fr),
    align: (center, left, left),
    table.header([*板块*], [*涵盖阶段*], [*本质内涵*]),
    [*分析部分* \ （前端主体）],
    [词法分析、语法分析、语义分析],
    [将源程序解构为细粒度成分，提取本质属性与结构依赖关系。],

    [*综合部分* \ （后端主体）],
    [中间代码生成、代码优化、代码生成],
    [将中间特征与目标架构结合，合成并构造等价的目标程序整体。],
)

=== 逻辑结构 vs. 物理执行顺序

*逻辑结构*（词法 $arrow.r$ 语法 $arrow.r$ 语义 $arrow.r$ 优化 $arrow.r$ 代码生成）是概念上的职能划分。

实际执行中各模块不一定严格串行先后结束。例如在单遍编译器中，通常以*语法分析为主控*，按需驱动词法分析读取 Token，并同步触发语义处理与局部代码生成。

== 编译程序的组织形式

=== 遍（Pass）

#definition[遍（Pass / 趟）][
    编译器完整扫描一次源程序（或其某种中间等价形式），并将其转换为另一阶段等价形式的独立过程。
]

#table(
    columns: (1.2fr, 2fr, 2fr),
    align: (center, left, left),
    table.header([*组织方式*], [*工作机制*], [*优缺点分析*]),
    [*单遍扫描* \ （Single-Pass）],
    [以语法分析程序为主控，一边读取分析，一边生成目标代码。],
    [*速度快、内存开销小*；但无法做全局深度优化，语言文法必须支持即读即译（如 Pascal 强制先声明后使用）。],

    [*多遍扫描* \ （Multi-Pass）],
    [每遍完成特定任务并把中间结果输出（内存/磁盘），后续遍继续读取。],
    [*结构模块化解耦、利于充分优化、各遍可独占内存*；但增加了中间表示的构建与 I/O 开销。],
)

=== 前端与后端解耦架构

现代主流编译器均采用“前端（Front End）+ 中间表示（IR）+ 后端（Back End）”的解耦体系：
- *前端*：与源语言强相关，与目标机无关的部分。涵盖词法、语法、语义分析及与机器无关的中间代码生成。
- *后端*：与目标机强相关，与源语言无关的部分。涵盖机器相关的中间代码优化、寄存器分配与目标指令生成。后端不依赖于源语言而仅依赖于中间语言。


=== 运行平台划分

- *本地编译器*：编译器所运行的宿主机平台与目标代码运行的目标机平台完全一致。
- *交叉编译器*：输出的目标代码可以与编译器运行在不同的平台之上。如嵌入式开发所使用的riscv交叉编译。（为什么我的输入法能直接打出蔡朝晖？）

== 编译程序的构造与自展技术

=== 自编译性与自展技术（Bootstrapping）

若某种高级语言 $L$ 的编译器本身使用语言 $L$ 自行编写，该语言即具备*自编译性*。工程上通过自展技术（Bootstrapping）实现：
+ *种子阶段*：用机器语言或汇编语言实现语言 $L$ 的一个极小核心子集 $L_0$ 的编译器 $T_{L_0}$。
+ *步进自展*：
    - 用已实现的语言子集 $L_i$ 编写稍扩充后的更高级子集 $L_{i+1}$ 的编译器源码 $T_{L_{i+1}}^{L_i}$；
    - 使用上一轮生成的编译器 $T_{L_i}$ 编译该源码，得到可运行的 $T_{L_{i+1}}$。
+ *最终形成*：由小到大像滚雪球一样逐步扩展，最终获得完整语言 $L$ 的全功能自编译编译器。
// 这里需要仔细审核一下。ppt p65。

// === 编译器的移植与 T 型图（T-Diagrams）

// 编译器可用三元符号 $T(S, I, T)$ 描述，分别表示：源语言 $S$（Source）、实现运行语言 $I$（Implementation）、目标语言 $T$（Target）。

// 移植核心：利用宿主机 $A$ 上的现有编译器，为新硬件 $B$ 生成编译器可执行体（通常先编写以 $B$ 为目标的交叉编译器，再用其生成运行于 $B$ 的本地编译器）。

// === 自动化构造工具链

// 现代编译系统通常无需全手写，依赖形式化生成工具：
// - *词法分析器生成器*：UNIX 的 `Lex`、GNU 的 `Flex`（基于正规表达式与有限自动机理论）。
// - *语法分析器生成器*：UNIX 的 `Yacc`、GNU 的 `Bison`（基于上下文无关文法与 LALR 分析算法）。

== 与编译器相关的语言处理系统

编译器通常并非孤立存在，而是完整的语言处理流水线的一环：

```text
源程序 (.c/.cpp)
   │
   ▼  [预处理器 Preprocessor] ── 宏展开、头文件包含、条件编译
预处理后源码 (.i)
   │
   ▼  [编译器 Compiler]       ── 词法、语法、语义、优化、代码生成
汇编代码 (.s/.asm)
   │
   ▼  [汇编器 Assembler]      ── 将助记符指令翻译为机器目标模块
可重定位目标模块 (.o/.obj)
   │
   ▼  [链接器 Linker]         ── 解析外部引用、合并库文件与多个模块
可执行文件 (.exe/ELF)
   │
   ▼  [加载器 Loader]         ── 分配地址空间、载入内存、初始化执行
内存中可执行程序
```

- *辅助工具链*：包括调试器（Debugger）、性能分析器（Profiler）、集成开发环境（IDE）等，共同构建现代软件开发基础设施。
