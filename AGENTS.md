# 本项目笔记写作范式（从 chapter1 对比中提炼）

适用范围：为 `chapters/*.typ` 生成或补全《编译原理》课程笔记时。

## 一、权威原话不可改写，只能围绕它搭结构

- `#unim[...]` 是课程讲义的标准定义，**原样保留**。不得改写、不得删除、不得"润色"。
- PPT / 原稿已有的句子，措辞一律沿用。例：`输出的目标代码可以与编译器运行在不同的平台之上`，不要改成更学术的表述。
- 老师口语化的句子是笔记锚点，不是瑕疵。例：`把字符流切成一个一个有意义的"单词"`。
- 改写权限仅限三件事：加标题、加表格、调顺序。

## 二、结构可以新建，解释不许新增

- **加**：对比表、分层标题、条目化对照、`#definition` / `#example` 复用。
- **减**：意义升华、价值论证、"本质内涵"式点评、`M×N → M+N` 这类自造分析。
- 判断标准：这句话是在**整理**已给的信息，还是在**生产**新信息？后者删掉。

## 三、示例够用即停，不追求闭环

- 一个例子只给 PPT 上出现过的那份形态。不要为展示六阶段完整，就给同一表达式配六份产物（IR、优化后 IR、目标汇编……）。
- 只在 PPT 有出处的地方给具体例子；自造的推演一律不写。

## 四、没把握就留 TODO，不要自行圆场

```typst
// 这里需要仔细审核一下。ppt p65。
```

- 写一段看似完整的解释，不如写内容 + 显式标注待核出处。
- 存疑内容优先标注，不优先补全。

## 五、超纲 / 待定内容用注释挂起，不要删除

- 用 `//` 注释整段，而非删除。保留可回溯的组织形式，便于后续开学或核实时启用。

## 六、短段落、多换行、保留人味

- 宁可一行一句，不写长段。
- 不消除个人语气、吐槽、联想（如 `ez,不谈`、输入法段子）。
- 不做教科书中立腔调的"统一"。

## 七、忌「关键词加粗＋多级无序列表」通篇铺开

反例（用户点名的）：

```typst
- *符号表管理（Symbol Table Management）*：
    记录源程序中出现的每一个标识符及其属性……
- *出错处理（Error Handling）*：
    准确定位错误位置并输出诊断信息。
    - *词法错误*：非法字符……
    - *语法错误*：括号不匹配……
```

这种「段首加粗关键词 → 分多级无序列表 → 列表内每句开头再加粗关键词」的结构，**偶尔用可以，通篇如此就是百科全书的腔调**，不是写给人看的笔记。

- 替代写法：用正常句子叙述，靠「一条是……另一条是……」「先是……然后……」这类连接词承载层级，而不是靠 `-` 和 `*` 缩进。
- 判断标准：**层级是内容本身自带的（如六个阶段、自展的先后步骤），才配用列表；只是「话分两头说」的并列，写成一两句话即可。**
- 不要追求全篇对称的完整结构。

## 八、语言与校验

- 中文为主，术语首次出现附英文。
- Typst 数学箭头用 `$arrow.r$`，不写 `$\rightarrow$`（会编译报错）。
- 改动后必须 `typst compile main.typ` 通过（退出码 0）方可声称完成。
